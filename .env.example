# OpenTelemetry Configuration
# Copy this file to .env and update with your actual values

# Service name that will appear in Dynatrace
OTEL_SERVICE_NAME=otel-nuxt-demo

# Dynatrace OTLP endpoint
# Replace YOUR_TENANT with your actual Dynatrace tenant ID
OTEL_EXPORTER_OTLP_ENDPOINT=https://YOUR_TENANT.live.dynatrace.com/api/v2/otlp

# Dynatrace API Token
# Replace YOUR_API_TOKEN with your actual token (requires metrics.ingest scope)
# To create a token: Dynatrace > Access tokens > Generate new token
OTEL_EXPORTER_OTLP_HEADERS=Authorization=Api-Token YOUR_API_TOKEN

# Optional: Path normalization for span names
# Use JSON array format: ["pattern", "replacement"]
# Examples:
# - Normalize locale paths: NUXT_OPENTELEMETRY_PATH_REPLACE='["^/(en|de|fr)/", "/:locale/"]'
# - Normalize API versions: NUXT_OPENTELEMETRY_PATH_REPLACE='["^/v[0-9]+/", "/v:version/"]'
# NUXT_OPENTELEMETRY_PATH_REPLACE='["^/(en|de|fr)/", "/:locale/"]'
